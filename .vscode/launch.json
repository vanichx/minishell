{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "Run Minishell",
            "program": "${workspaceFolder}/minishell",
            "args": ["./minishell"],
            "cwd": "${workspaceFolder}",
        }
    ]
}